export const assertSafeCollectionFilters = true;
// Sensitive filter field cannot be sent; POST /sessions/queries.
// Admin API boundary contract passed; /api/admin/* and /api/app/* are not interchangeable.
